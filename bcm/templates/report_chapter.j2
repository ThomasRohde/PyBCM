You are a highly skilled writing assistant. Your objective is to produce the full content for:
- Chapter: {{ capability_name }}
- Report subject: {{ organisation_name }}
- Subject description: {{ organisation_description }}
- Context: {{ context }}

## Instructions & Constraints

1. **Purpose**: Provide a detailed, comprehensive write-up of the specified chapter spanning multiple sections.
2. **Formatting**:
   - Return the result as a JSON array.
   - Each JSON object should include:
     - `"name"`: a concise but descriptive title of the section.
     - `"description"`: a full, multi-paragraph write-up of the section's content, incorporating all relevant details and context.
   - Example:
     ```json
     [
       {
         "name": "Section 1 header",
         "description": "Paragraph 1...\n\nParagraph 2..."
       },
       {
         "name": "Section 2 header",
         "description": "Paragraph 1...\n\nParagraph 2..."
       }
     ]
     ```
3. **Language Usage**:
   - Avoid self-referential phrases (e.g., "In this chapter" or "This chapter will cover").
   - Maintain a professional, clear, and informative tone.
4. **Output Requirements**:
   - **Only** return the JSON array (no markdown, headings, or extra commentary).
   - Use escaped whitespace in the JSON, e.g. for newlines
   - Ensure the JSON is valid.
   - Each `"description"` must provide full, coherent text reflecting the given chapter and context.
5. ** JSON Output format: **

```
[
  {
    "name": "Section 1",
    "description": "Content of section 1",
  },
  {
    "name": "Section 2",
    "description": "Content of section 2",
  },
  {
    "name": "Section 3",
    "description": "Content of section 3",
  },
  {
    "name": "Section 4",
    "description": "Content of section 4",
  }
]
```